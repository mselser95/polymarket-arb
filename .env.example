# Polymarket API Credentials
# Copy this file to .env and fill in your credentials

# Builder API credentials (created via Polymarket UI or derive-api-creds command)
POLYMARKET_API_KEY=
POLYMARKET_SECRET=
POLYMARKET_PASSPHRASE=

# Your Ethereum private key (without 0x prefix)
# SECURITY WARNING: Never commit your private key to git!
POLYMARKET_PRIVATE_KEY=

# Optional: Your EOA address (auto-derived from private key if not provided)
POLYMARKET_ADDRESS=

# Optional: Proxy wallet address (if using Gnosis Safe)
POLYMARKET_PROXY_ADDRESS=

# Signature type: 0=EOA (default), 1=POLY_PROXY, 2=POLY_GNOSIS_SAFE
POLYMARKET_SIGNATURE_TYPE=0

# How to obtain credentials:
# 1. Go to https://polymarket.com and connect your wallet
# 2. Get Builder API credentials:
#    - Option A: Use the UI at https://polymarket.com/settings/api
#    - Option B: Run: go run . derive-api-creds
# 3. Copy your private key from MetaMask (Account Details -> Export Private Key)
#
# To use these credentials:
#   cp .env.example .env
#   # Fill in your credentials in .env
#   go run . place-orders <market-slug> --size 1.0 --yes-price 0.50 --no-price 0.50
